/**
 * Current Weft package version. Useful for diagnostics, telemetry, discovery
 * documents, and verifying which build is running.
 *
 * @example
 * ```ts
 * import { VERSION } from 'weft';
 *
 * console.log(`Running Weft ${VERSION}`);
 * ```
 */
export const VERSION = '0.1.0';
